Neural network
	Sigmoid (logistic) activation function : h_theta(x) = 1/(1+exp(-theta'*x))
	"For a neural network with only two layer (no hidden layers) the model is exactly the same as logistic regression."
